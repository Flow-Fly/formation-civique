Completed the `cr` phase-1 batch in [question-fiche-links.cr.r008.json](/Users/flow/Documents/github/repositories/formation-civique/data-init/suggestions/question-fiche-links.cr.r008.json) and set `workflowStatus` to `completed`. I linked the school, parental-rights, majority, laïcité, and handicap questions to fiche ids justified by the fiche text, and kept `crx122`, `crx134`, `crx141`, and `crx148` with empty `relatedFicheIds` because the current fiche set does not state those answers explicitly. I did not create another batch file or touch `data-init/question-bank.json`.

I validated the JSON with `jq` and refreshed [qcm-workflow-state.json](/Users/flow/Documents/github/repositories/formation-civique/data-init/suggestions/qcm-workflow-state.json). The workflow `nextAction` is still `continue_linking`; `cr` is now at 205/209 linked, with `crx122`, `crx134`, `crx141`, and `crx148` still marked `needs_review`, and the next recommended batch id is `r009`.